# Scenario: review-checkpoint-satisfied-by-label

Verifies the label-based satisfaction path. Redline reports a
triggered `api-review` checkpoint with `satisfied: true` because a
maintainer applied the `api-reviewed` label. `review.checkpoints_
satisfied` passes; overall verdict clean.
