# Scenario: approval-high-clean-context-as-approval

High record where the same reference text appears in both Plan review
and Approvals (modulo whitespace). Predicate
`approval.clean_context_does_not_satisfy_human` blocks; it is also
non-waivable.
