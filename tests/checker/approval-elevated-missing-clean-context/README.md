# Scenario: approval-elevated-missing-clean-context

Elevated record whose Plan review field is just `self` and no
`## Plan review` section is present. Predicate
`approval.elevated_clean_context_review_present` blocks.
