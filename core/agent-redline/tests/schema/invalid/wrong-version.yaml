# Invalid: wrong version (must be 1)
version: 2

project:
  name: wrong-version

zones:
  red:
    - path: src/main/java/**/domain/**
      reason: domain
      checkpoint: architecture-review

checkpoints:
  architecture-review:
    satisfiedBy:
      - codeownerApproval
