# CodeRabbit harness defects (A–H)

<!-- agent-workflow:start -->
**Outcome:**
Eight confirmed defects (A–H) in the agent-workflow harness source-of-truth are fixed, the vendored `dist/` is regenerated, and the test suite is green. Two behavioral fixes (A glob, C exit code) carry regression tests. Ported from the upstream fix; validated to fit this public repo (no host-specific `$id`/terms introduced).

**Target:**
This repository. Source-of-truth files under `core/…` and top-level `scripts/…`; generated copies in `dist/` and `.claude/` regenerated by the packager.

**Scope:**
- A/E: `core/agent-redline/core/reporter/reporter.py` (`_codeowners_match` + new `_codeowners_glob_to_regex`, `resolve_boundary_input` guard in main(), suppressions-config mapping guard)
- B/F: `scripts/format-verdict-comment.py` (`_explain_satisfy`, `_record_age`)
- C: `core/agent-redline/extensions/python/scripts/run-import-linter.py` (config-error exit code + empty report; `handle_report`/`_config_error_messages` helpers)
- D: `scripts/agent-workflow-tune.py` (`main()` JSONDecodeError/KeyError/TypeError fallback)
- G: `core/skill/hooks/merge-agents-section.py` (CRLF preservation + duplicate-marker detection)
- H: three schema files under `core/agent-redline/core/schema/` — mojibake escapes in `description` text ONLY; each file's `$id` (this repo's own host) is left untouched
- Work-Record principle reword: `core/skill/hooks/seed-workflow.sh` CTX (every *task* gets a Work Record; guarded paths are only the plan-mode enforcement subset)
- Tests: `core/agent-redline/tests/reporter/test_reporter_unit.py` (A) + new `core/agent-redline/tests/extensions/python/test_run_import_linter.py` (C)
- Regenerated: `dist/agent-workflow/**` + tracked `.claude/hooks/` (via `scripts/package-skill.sh`)

**Constraints:**
- Fix source-of-truth ONLY; never hand-edit `dist/` or `.claude/` copies — regenerate via packager.
- No semantic change to any JSON-schema validation contract (H is display-text only); preserve each schema's existing `$id`.
- Preserve existing reporter behavior (anchored vs unanchored CODEOWNERS matching; goldens unchanged).
- Port carries NO host-specific or environment-specific terms; changes are generic harness logic.

**Completion criteria:**
- `bash tests/run-all.sh` green.
- New A tests: `foo/**/bar` matches `foo/bar` / `foo/x/bar` / `foo/x/y/bar`; `foo/**bar` stays within-segment.
- New C test: invalid_contract_options path returns exit 2 with an empty report.
- `tests/package` drift check passes → `dist/` regenerated and committed.

**Risk:** Elevated

**Complexity:** Moderate

**Reason:**
Red-zone schema files (`core/agent-redline/core/schema/**`, architecture-review checkpoint) are in scope → Elevated floor. NOT High: schema edits are non-semantic (double-encoded Unicode in `description` strings only); all other files are watch/blue zones. Moderate: 8 discrete fixes across ~7 files + tests + re-vendor, single repo.

**Discovery:**
- OSS baseline is byte-identical to the upstream pre-fix baseline for all code files (reporter, format-verdict, run-import-linter, tuner, merge-agents-section, test_reporter_unit) → the upstream logical edits apply verbatim, no divergence.
- `seed-workflow.sh` CTX here reads "changes that modify code under a guarded path require a Work Record" — the exact contrapositive the reword fixes ("not guarded → no record"). Reworded to lead with the per-task base principle.
- Schema files differ from upstream ONLY in `$id` (this repo's own host) — so H is applied as targeted `description`-text edits, NOT a file copy.
- `core/agent-redline/tests/extensions/python/` exists (check-extension.sh) → new C unit test lands there.
- Redline policy classifies `core/agent-redline/core/schema/**` red (architecture-review) — matches upstream; drives the Elevated classification.

**Material assumptions:**
- Assumption: the C unit test (duck-typed fake Report) needs no import-linter install. Disproof: an ImportError at collection. Action: test loads the adapter by path and calls `handle_report`/`_config_error_messages` only — never `main()`.
- Assumption: re-vendoring reproduces the same `dist/` deltas as upstream. Disproof: `tests/package` drift. Action: run `scripts/package-skill.sh` then the package layer.

**Plan:**
1. Port the 6 pure-code files verbatim (identical baseline, no host terms).
2. Add the new C unit test file.
3. Apply mojibake-only `description` edits to the 3 schemas (preserve `$id`).
4. Reword `seed-workflow.sh` CTX + add the printf-JSON guard comment.
5. Re-vendor via `scripts/package-skill.sh`; sync tracked `.claude/hooks/`.
6. Run full suite + reporter-unit + new C test.

**Verification plan:**
- A `**` glob matches zero/one/multi segments and stays within-segment when glued to a literal → pytest `TestCodeownersGlobMatching` new cases.
- C config error yields exit 2 and an empty report → `test_run_import_linter.py` exit-code cases.
- B/D/E/F/G behavioral fixes → targeted unit/CLI checks.
- H schema mojibake removed → jsonschema loads all three and em dash / section-sign render; `$id` unchanged.
- Whole suite stays green → `bash tests/run-all.sh` (package layer confirms `dist/` regenerated).

**Plan review:** self (Elevated — localized defect fixes ported from a reviewed, merged upstream change with tests; developer in the loop)

**Approvals:** Not required at this risk level

**Exceptions:** —

**State:** Ready for review
<!-- agent-workflow:end -->

## Implementation

Port of a merged upstream fix (CodeRabbit review of a downstream install). Baseline verified identical to upstream pre-fix for all code files; only the schema `$id` and the seed-hook CTX wording differ, both handled by targeted edits rather than file copies so nothing host-specific is introduced.

## Evidence

- Port validated as identical to the reviewed upstream fix for every code file; the only differences are the three schema `$id` lines, which correctly keep this repo's own host. No host-specific or environment-specific terms introduced (grepped the ported files clean).
- Full suite green: `bash tests/run-all.sh` → **EXIT=0**, all layers ok (budget, schema, work-record, checker, redline, tuner, hooks, links, package). The `package` layer confirms `dist/` matches source; the `hooks` layer confirms the tracked `.claude/hooks/` dogfood copies match `dist/`.
- Reporter unit + new import-linter unit: **136 passed** (`test_reporter_unit.py` + new `test_run_import_linter.py`). A cases cover zero/one/multi-segment `**` and `**` glued to a literal staying within-segment; C cases cover exit 2 + empty report on config error, exit 1 broken contract, exit 0 clean.
- Schemas re-parse as valid JSON; em dash / section-sign render; each `$id` unchanged.

Vendored artifacts regenerated via `scripts/package-skill.sh` (`dist/agent-workflow/**` + tracked `.claude/hooks/`).
