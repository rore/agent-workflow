# Consumer workflow runner default (self-hosted → ubuntu-latest)

<!-- agent-workflow:start -->
**Outcome:**
The consumer CI workflow template defaults to `runs-on: ubuntu-latest` instead of `[self-hosted]` (both jobs), with `docs/INTEGRATION.md` reframing self-hosted as the enterprise alternative. `dist/` regenerated so the shipped template + manifest match source. Test suite green.

(Originally this task also covered a repo-wide CRLF→LF renormalization; during Discover that finding was disproven as a measurement artifact — see Discovery. Scope reduced to the runner default only.)

**Target:**
This repository. Source-of-truth `core/templates/.github/workflows/agent-workflow.yml.template` + `docs/INTEGRATION.md`; generated `dist/agent-workflow/**` + tracked `.claude/` copies regenerated by the packager.

**Scope:**
- Runner default: `core/templates/.github/workflows/agent-workflow.yml.template` — both `runs-on: [self-hosted]` (redline job line 44 + agent-workflow job line 250) → `runs-on: ubuntu-latest`. (Plan review confirmed there is no adjacent self-hosted comment to reframe — the change is the two scalar values only.)
- Docs: `docs/INTEGRATION.md:9` (the "self-hosted … default in the shipped template" line) reframed so ubuntu-latest is the default and self-hosted the enterprise alternative.
- Regenerated: `dist/agent-workflow/**` + tracked `.claude/` copies via `scripts/package-skill.sh`.
- OUT OF SCOPE (explicit, per plan review): `.github/workflows/test.yml:19` self-hosted mention — a comment explaining why `setup-python` is used, not a runner default, accurate on hosted runners. `core/skill/bootstrap-mode.md:222` "runner labels" is generic guidance, not a default.

**Constraints:**
- Fix source-of-truth ONLY; never hand-edit `dist/` — regenerate via the packager so `manifest.txt` stays honest.
- The repo's own live `.github/workflows/agent-workflow.yml` already uses `ubuntu-latest`; unchanged.
- Preserve the template's structural invariants (`needs: [redline]`, verdict artifact upload/download @v4, `--changed-files`, `--redline-verdict`, captured exit codes, distinct sticky headers).

**Completion criteria:**
- Template both jobs: `runs-on: ubuntu-latest`; `docs/INTEGRATION.md` frames self-hosted as the alternative; no residual self-hosted *default*.
- `dist/agent-workflow/templates/.github/workflows/agent-workflow.yml.template` shows `ubuntu-latest` after repackage.
- `bash tests/run-all.sh` green (via python3 shim), including the `package` drift check.

**Risk:** Elevated

**Complexity:** Simple

**Reason:**
CI workflow template (gray-zone governance) + guarded path `core/` in scope → Elevated (conservative default). NOT High: no contract/security/persistence/financial surface — runner label only. Simple: one coherent change (runner default + its doc), single session; `dist/` regeneration is mechanical. `(Elevated, Simple)` → expanded shape.

**Discovery:**
- **CRLF finding disproven (measurement artifact).** Initial investigation reported repo-wide CRLF in committed blobs via `grep -c $'\r'` on `git cat-file` output and on files. Authoritative re-check with `od -c` (raw bytes) shows **0 CR bytes** on disk and in the HEAD blob; `git ls-files --eol` reports `i/lf w/lf` for every file — **0** files CRLF/mixed repo-wide (677 `i/lf`, 26 `i/none`). The phantom CRs were injected by the Windows/MSYS layer when piping `git cat-file`/reads through `grep -c $'\r'`. `.gitattributes eol=lf` is already in force. `git add --renormalize` and a forced `rm --cached`+`add` both correctly staged **0** files — nothing to normalize. No renormalization needed or done.
- Runner default is a genuine content fact (read directly from the YAML, not CR-counted): `runs-on: [self-hosted]` at template `:44` (redline job) and `:250` (agent-workflow job), present since initial release. The repo's own live CI (`.github/workflows/agent-workflow.yml:33,187`) and every extension scaffold use `ubuntu-latest`; `docs/INTEGRATION.md:9` documents self-hosted as the shipped default. self-hosted is an internal-context carryover — wrong default for a public template (consumers without self-hosted runners get jobs that queue forever).
- `package-skill.sh` reads the working tree and writes substituted markdown as LF; `cp`'d files inherit source endings (already LF). `tests/package/check-package.sh` uses `diff -r --strip-trailing-cr` (EOL-agnostic on contents); the manifest tracks byte counts, so regenerate rather than hand-edit.
- `python3` is broken on this machine and `package-skill.sh` hardcodes it → use the `/tmp/pyshim` python3→CPython shim to run the packager + tests.

**Material assumptions:**
- Assumption: flipping the two `runs-on` scalars is the complete public-default fix. Disproof: another source file hardcodes/documents self-hosted as a default. Action: plan review inventoried sources — only the two template lines + `docs/INTEGRATION.md:9` (test.yml:19 is a comment, out of scope). Confirmed complete.
- Assumption: `ubuntu-latest` matches the repo's own dogfooding and the extension scaffolds. Disproof: repo CI uses something else. Action: verified `.github/workflows/agent-workflow.yml` already uses `ubuntu-latest`.

**Plan:**
1. Edit the template's two `runs-on: [self-hosted]` → `runs-on: ubuntu-latest` (both jobs).
2. Reframe `docs/INTEGRATION.md:9` so ubuntu-latest is the default and self-hosted the enterprise alternative.
3. Regenerate `dist/` via `scripts/package-skill.sh` (python3 shim); `git add` source + generated.
4. Run the full suite green (`package` drift confirms dist matches source).
5. Single commit; push, open PR, watch CI, resolve review threads before merge.

**Verification plan:**
- Runner default → `grep` template (source + dist) shows both jobs `ubuntu-latest`; no residual self-hosted default outside the docs' "alternative" mention.
- No semantic drift beyond intent → `git diff` shows only the 2 template lines, the doc reframing, and the regenerated `dist/` copies of the same.
- `package` drift → `tests/package/check-package.sh` exit 0 (dist matches source).
- Whole suite → `bash tests/run-all.sh` (python3 shim) EXIT 0.

**Plan review:**
Clean-context read-only agent review recorded under `## Plan review` below. Verdict: **sound-with-nits**, no blockers. NOTE: the review covered the original (larger) plan including renormalization; that portion is now moot (finding disproven). The runner-default portion — the only remaining work — was reviewed as sound and inventory-complete. Scope was *reduced*, not expanded, so no re-planning gate triggered.

**Approvals:** Not required at this risk level

**Exceptions:** —

**State:** Ready for review
<!-- agent-workflow:end -->

## Implementation

- **Discover:** The headline "repo-wide CRLF" issue was disproven as a Windows/MSYS `grep -c $'\r'` pipe artifact (`od -c` + `git ls-files --eol` show the repo is already pure LF; renormalization staged 0 files). Scope reduced from two workstreams to one — the self-hosted runner default, which is a real content fact.

## Evidence

- **Full suite green:** `PATH=/tmp/pyshim:$PATH bash tests/run-all.sh` → **all layers ok** (budget, schema, work-record, checker, redline, tuner, hooks, links, package). The `package` layer confirms `dist/agent-workflow/` matches source after regeneration.
- **Runner default flipped:** both jobs `runs-on: ubuntu-latest` in source (`core/…:44,250`), regenerated `dist/…`, and synced `.claude/skills/…`. `docs/INTEGRATION.md:9` reframes self-hosted as the enterprise alternative; the only remaining `self-hosted` in `core/`+`docs/` is that intentional "change to `[self-hosted]`" alternative mention.
- **Minimal, no unintended drift:** Four implementation files changed (excluding this Work Record): `core/` template, `dist/` template, `.claude/skills/` template, `docs/INTEGRATION.md`. `manifest.txt` is unchanged because `ubuntu-latest` and `[self-hosted]` are both 13 chars → identical byte counts.
- **No renormalization performed:** the CRLF premise was disproven (`od -c` raw bytes + `git ls-files --eol` → repo already pure LF, 0 files CRLF/mixed). Renormalize attempts correctly staged 0 files.

## Plan review

Clean-context read-only agent review (Explore agent, no prior context from the planning session; read the Work Record + packager + drift gate + `.gitattributes` + template + docs fresh).

**Verdict: sound-with-nits.** No correctness blocker. (Reviewed the original plan, which also included the renormalization workstream since disproven; findings on the runner-default change stand.)

- **Runner flip complete + invariant-preserving.** Only two source `self-hosted` occurrences (template `:44`, `:250`); flip to scalar `ubuntu-latest` is valid YAML, matches the repo's own live workflow, and touches no structural invariant (`needs`, `if: always()`, artifacts @v4, `--changed-files`, `--redline-verdict`, exit codes, sticky headers). `ubuntu-latest` is the right value.
- **Inventory complete.** Only `docs/INTEGRATION.md:9` documents self-hosted as the default. Extension scaffolds already use `ubuntu-latest`. `.github/workflows/test.yml:19` is a comment, not a default → out of scope. `bootstrap-mode.md:222` "runner labels" is generic → nothing actionable.
- **dist regeneration required (not hand-edit)** — `check-package.sh:30` uses `diff -r --strip-trailing-cr`; manifest byte counts are the ending-sensitive artifact, so regenerate via the packager.
- (Renormalization findings — no binaries at risk, no CRLF-dependent fixtures, correct ordering — are moot now that the CRLF premise was disproven.)