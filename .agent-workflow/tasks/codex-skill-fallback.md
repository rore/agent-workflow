# Add Codex skill fallback to AGENTS.md template

<!-- agent-workflow:start -->
**Outcome:** Consumer AGENTS.md sections include a fallback for runtimes (e.g. Codex) that don't auto-discover `.claude/skills/`. Those agents can read the skill directly from the committed path instead of failing silently.

**Target:** This repo. Template source + dist/ regeneration.

**Scope:** `core/templates/agents-section.md.template` — append fallback sentence to the `To start a task:` line. `dist/agent-workflow/templates/agents-section.md.template` and `dist/agent-workflow/manifest.txt` — regenerated by `package-skill.sh`. No changes to skill logic, checker, or CI.

**Constraints:** Template file must stay within 400-token budget ceiling. No consumer doc changes needed — bootstrap re-runs will pick up the new template text automatically.

**Completion criteria:** All 9 test layers green. Template contains fallback sentence. dist/ matches source. CI green.

**Risk:** Elevated

**Complexity:** Simple

**Reason:** Touches watch-zone skill source (`core/templates/`) and `dist/` paths → detected Elevated by redline. Single sentence addition to a template → Simple.

**Discovery:** Codex sessions can't discover `.claude/skills/` because Codex has its own skill-discovery path. The skill is committed there for Claude Code but invisible to Codex. Fallback sentence directs any runtime to the committed path directly. The 400-token ceiling required trimming one word ("skill") from the phrase "relative to that directory".

**Material assumptions:**
- A1: The committed `.claude/skills/agent-workflow/SKILL.md` path is stable across bootstrap versions. Disproof: bootstrap changes the install path.

**Plan:** 1. Edit template. 2. Run package-skill.sh. 3. Verify budget. 4. Run all tests. 5. PR.

**Verification plan:** `bash tests/run-all.sh` green (all 9 layers). Template line 20 contains fallback. CI green.

**Plan review:** clean-context agent review recorded under `## Plan review` below.

**Approvals:** Not required at this risk level (Elevated). Clean-context review satisfies the gate; no red-zone checkpoint fires on this change.

**Exceptions:** —

**State:** Ready for review
<!-- agent-workflow:end -->

## Implementation

Single sentence appended to the `To start a task:` line in `core/templates/agents-section.md.template`. One word trimmed to stay within the 400-token budget. `package-skill.sh` regenerated dist/. All 9 test layers green locally.

## Plan review

Clean-context review (read-only; no prior context from the implementation session).

**Verdict: sound.** The change is a one-sentence addition to a template file. The fallback path `.claude/skills/agent-workflow/SKILL.md` is the canonical committed location installed by bootstrap — stable, correct. The 400-token trim is verified by the budget layer. No skill logic, checker, CI, or consumer contracts are touched. `dist/` is regenerated from source. All 9 layers pass. No issues.
