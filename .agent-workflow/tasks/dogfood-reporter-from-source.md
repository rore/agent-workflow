# Dogfood the redline reporter from source (remove the stale repo-root copy)

<!-- agent-workflow:start -->
**Outcome:**
The `redline` CI job runs the reporter from source (`core/agent-redline/core/reporter/reporter.py`), matching how the `agent-workflow` job already dogfoods the checker (`python -m core.checker`). The stale, hand-vendored `scripts/agent-redline-report.py` is deleted. Drift of the repo's own reporter becomes structurally impossible, and CI now exercises the current reporter (including the `.agent-workflow/**` exclusion).

**Target:**
This repo. CI workflow + deletion of one orphaned vendored script + this-repo doc rows.

**Scope:**
- `.github/workflows/agent-workflow.yml`: the `Run agent-redline reporter` step invokes `python core/agent-redline/core/reporter/reporter.py` instead of `python scripts/agent-redline-report.py`; all flags unchanged. A comment notes this dogfoods the reporter from source, mirroring the checker.
- Delete `scripts/agent-redline-report.py` (orphaned pure-copy of the core reporter; never regenerated by `package-skill.sh`; only the CI line invoked it).
- `AGENTS.md`: update the two "This repo" rows that described the reporter as vendored at repo root, to the from-source shape (parallel to the existing checker row). Consumer-scoped docs (`docs/INTEGRATION.md`, `docs/PACKAGING.md`, `docs/REDLINE.md`) stay — consumers still vendor the reporter from `dist/`.

**Constraints:**
- No change to `package-skill.sh` or the `dist/`/skill reporter copies — consumers still vendor the reporter from `dist/`. Only this repo's own CI invocation changes.
- Reporter must run standalone as a script (self-contained, stdlib+PyYAML single file; PyYAML present in CI, used by the checker).
- Red-zone touch (`.github/workflows/agent-workflow.yml`) → `architecture-review` checkpoint fires (advisory under shadow mode).

**Completion criteria:**
- `bash tests/run-all.sh` green (no `dist/` change; the orphan is not part of the package drift check).
- CI `redline` step references `core/agent-redline/core/reporter/reporter.py`; `scripts/agent-redline-report.py` no longer exists; no broken markdown link to it.
- `python core/agent-redline/core/reporter/reporter.py --policy agent-redline-policy.yaml --changed-files <f> --json-out <o>` produces a verdict reflecting the `.agent-workflow/**` exclusion.

**Risk:** Elevated

**Complexity:** Simple

**Reason:**
Touches the red-zone CI workflow (`.github/workflows/agent-workflow.yml`, `architecture-review` checkpoint) → Elevated (not High: CI-gate config, not a contract/security/persistence/financial data surface). Simple: swap one invocation line + delete one orphaned file + two this-repo doc rows, single session.

**Discovery:**
- The `agent-workflow` job runs the checker in-process from source (`python -m core.checker`) — no committed repo-root checker exists. The `redline` job runs `python scripts/agent-redline-report.py`, a committed vendored copy.
- `scripts/agent-redline-report.py` is a pure copy of `core/agent-redline/core/reporter/reporter.py` but `package-skill.sh` only writes the reporter into the `dist/`/skill TARGET, never the repo-root copy — so it drifted and lacks the `.agent-workflow/**` exclusion.
- The only *executable* invocation of the orphan is the CI `redline` step. Prose references in `AGENTS.md` (this repo's setup) are updated; consumer-scoped mentions in `docs/INTEGRATION.md` / `docs/PACKAGING.md` / `docs/REDLINE.md` stay, since consumers still vendor the reporter.
- The source reporter runs standalone (`python core/…/reporter.py --help` works — self-contained single file with a `__main__` CLI).

**Material assumptions:**
- A1: Running the reporter by its repo-root path resolves correctly in CI (self-contained; no local package imports; cwd is the repo checkout, where `changed-files.txt` / `agent-redline-policy.yaml` already resolve). Disproof: CI `redline` step errors on import/path.
- A2: PyYAML is available in CI (the checker depends on it and runs there). Disproof: reporter errors on `import yaml`. The reporter guards the import; policy is YAML so yaml is required regardless.

**Plan:**
1. Edit `.github/workflows/agent-workflow.yml` `Run agent-redline reporter` step to invoke the source reporter; add the dogfood comment.
2. `git rm scripts/agent-redline-report.py`.
3. Update the two this-repo `AGENTS.md` rows.
4. Run `bash tests/run-all.sh` (links layer catches any missed doc link); smoke-run the source reporter.
5. Clean-context review; PR.

**Verification plan:**
- CI dogfoods current reporter → `redline` step runs `core/agent-redline/core/reporter/reporter.py` (grep the workflow).
- Orphan removed → `scripts/agent-redline-report.py` absent; no broken link (links layer).
- Reporter runs from source → `python core/agent-redline/core/reporter/reporter.py --policy … --changed-files … --json-out …` exits cleanly; a WR-only diff is not gray.
- No regressions → `bash tests/run-all.sh` green.

**Plan review:** clean-context agent review recorded under `## Plan review` below.

**Approvals:** Not required at this risk level

**Exceptions:** —

**State:** Ready for review
<!-- agent-workflow:end -->

## Implementation

Removes the last drift vector for the harness's own reporter: the `redline` CI job now runs the reporter from source, exactly as the `agent-workflow` job already dogfoods the checker, and the orphaned repo-root vendored copy is deleted. Red-zone CI touch → architecture-review checkpoint applies (advisory under shadow mode).

## Plan review

Clean-context agent review (read-only; no prior context from the implementation session).

**Verdict: sound.** Verified independently:
- All five CI flags (`--policy/--changed-files/--pr-labels/--codeowner-approvals/--json-out`) are valid against the current reporter `main()`. Ran the source reporter three ways: WR-only diff → BLUE with 1 excluded file (the `.agent-workflow/**` exclusion the stale copy lacked); full PR change set → RED (red zone `.github/workflows/agent-workflow.yml`, `architecture-review` unsatisfied, exit 1 advisory under shadow); emitted keys are the full shape the downstream `agent-workflow` job reads via `redline_verdict._from_mapping`.
- Only the CI `redline` step invoked the deleted file (no active pre-push hook). The remaining `AGENTS.md` mention is correctly in the *consumer* column; every other reference (`docs/INTEGRATION.md`/`PACKAGING.md`/`REDLINE.md`, `dist/**`, `core/templates/**`, `package-skill.sh`, tests, historical Work Records) is consumer-scoped and correct to leave. `links` layer passes (no broken link).
- Consumers unaffected: `dist/…/agent-redline-report.py` is byte-identical to source; `package-skill.sh` (unchanged) still vendors it. `package` layer passes.
- Elevated/Simple defensible; no import/cwd/PyYAML risk (`python` is the established interpreter in this workflow; PyYAML present in CI).
- Premise validated: the deleted copy was 1545 vs the source's 1997 lines — CI was demonstrably running stale reporter code.
- `bash tests/run-all.sh` green across all 9 layers.

No code changes recommended.

