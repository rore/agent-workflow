<!-- agent-workflow:start -->
**Outcome:**
Work Record authors are warned that a bold Label at the start of a line inside the marker block is reserved for fields — the parser treats any such line as a field and rejects unknown ones — so bold sub-headings inside a field's prose value (e.g. Plan) will not silently break the record. The guidance lands as an HTML comment in both Work Record templates; dist/ and the tracked local install regenerated.

**Target:**
This repository. Source: `core/templates/work-record-routine.md`, `core/templates/work-record-expanded.md`. Generated `dist/agent-workflow/**` + `.claude/skills/agent-workflow/**` regenerated by `scripts/package-skill.sh`.

**Scope:**
Documentation only — one HTML comment near the start marker of each template. Then repackage. No parser/checker behavior change. (A parallel clause was also planned for `operating-mode.md` and the SKILL principle; dropped — see Discovery — because both agent-loaded files are at their token-budget ceilings and additions would require cutting existing content, which is out of scope.)

**Constraints:**
Fix source only; never hand-edit `dist/` — regenerate via the packager so `manifest.txt` stays honest. No change to `core/work_record/parser.py` or any predicate. Preserve the templates' existing field set and marker structure exactly. Do not push any agent-loaded file over its token budget.

**Completion criteria:**
Both templates carry the reserved-label note; `dist/` + `.claude/skills/` copies regenerated; `bash tests/run-all.sh` green including `budget`, `package`, and `links`; the agent-workflow CI checker passes (declared Risk not below detected).

**Risk:** Elevated

**Complexity:** Simple

**Reason:**
Initially declared Routine on a misreading of the zone rules — I read `core/templates/**` as watch-only and watch as Routine-eligible. It is not: the redline reporter reports watch-listed paths (`core/templates/**`, `core/**`, `dist/agent-workflow/**`, `.claude/skills/agent-workflow/**`) as gray, and `detected_risk()` returns Elevated for any gray touch. Only blue paths (`docs/**`, `tests/**`, `.agent-workflow/tasks/**`) are Routine-eligible. Detected = Elevated; declared corrected to Elevated (Risk may be raised, never lowered). Simple: one mechanical doc change, single session. `(Elevated, Simple)` → expanded shape + clean-context plan review.

**Discovery:**
- Motivated by a real papercut: a consumer (Pallium) Work Record failed CI when a bold sub-heading inside the Plan prose (`P1 carry-forward decision …`) was parsed as an unknown field. `core/work_record/parser.py` `_FIELD_HEADER_RE` (`^\*\*([^*]+?):\*\*`, re.M) matches any line-start bold-label; unknown ones raise `WorkRecordParseError` at `_validate_against_schema`. The parser is correct-by-design; only the docs need the constraint made explicit.
- Placement finding: the `budget` test layer enforces token ceilings on agent-loaded files. Adding the corollary to `core/skill/agent-workflow.md` (523/500) and `core/skill/operating-mode.md` (1956/1900) pushed both over. Reverted those; kept the templates-only note, which is the higher-value placement (authors see it inline while filling in fields) and is not budget-constrained.
- Classification finding: watch-listed paths detect as gray → Elevated (see Reason).

**Material assumptions:**
- Assumption: the HTML comment cannot itself be captured as a field. Disproof: the parser matches a `**Label:**` inside an HTML comment or the comment introduces a line-start bold-label. Action: comment contains no line-start `**X:**`; the token appears mid-sentence and inside `<!-- -->`. Confirmed by clean-context review + full suite green.
- Assumption: flipping only the two templates fully delivers the guidance. Disproof: authors don't read templates. Action: templates are copied and filled per operating-mode Step 3, so the comment is seen at authoring time — the correct delivery point.

**Plan:**
1. Add a concise HTML comment after `<!-- agent-workflow:start -->` in both templates (routine + expanded).
2. Regenerate `dist/` + `.claude/skills/` via `scripts/package-skill.sh` (python3 shim).
3. Run the full suite green.
4. Single implementation commit; PR; clean-context plan review (Elevated gate); resolve threads; merge.

**Verification plan:**
- Note present → `git diff` shows the comment in both source templates and their regenerated copies.
- No unintended drift → diff limited to the two templates + regenerated `dist/`/`.claude` copies + manifest byte counts.
- Budget respected → `budget` layer green (no file over ceiling).
- Dist matches source → `package` layer green.
- Links intact → `links` layer green.
- Whole suite → `PATH=/tmp/pyshim:$PATH bash tests/run-all.sh` EXIT 0.

**Plan review:**
Clean-context read-only agent review recorded under `## Plan review` below.

**Approvals:** Not required at this risk level

**Exceptions:** —

**State:** Ready for review
<!-- agent-workflow:end -->

## Implementation

- Discover/Assess: motivated by a real consumer papercut (see Discovery). Classified `(Elevated, Simple)` after the CI checker corrected an initial Routine mis-declaration — watch-listed template paths detect as gray → Elevated.
- Implement: added the HTML comment to both templates. Initially also edited `operating-mode.md` + the SKILL principle; the `budget` layer flagged both over ceiling, so those were reverted and the note kept templates-only.
- Verify: `bash tests/run-all.sh` green (all layers). `budget`, `package`, `links` confirmed individually as well.

## Plan review

Clean-context read-only agent (Explore) review, 2026-08-13. Verdict: **sound-with-nits**, safe to merge.
- Comment text verified accurate against `core/work_record/parser.py` (`_FIELD_HEADER_RE` line-start match; unknown label → `WorkRecordParseError` in `_validate_against_schema`).
- Minimal/scoped: exactly 9 files; the added hunk is byte-identical across all three copies of each template; manifest byte-counts are honest.
- No self-misparse risk: the comment's `**Label:**` is mid-line and inside `<!-- -->`, before the first real field; `_FIELD_HEADER_RE`'s `^\*\*` anchor cannot match it.
- Nit (applied): "an unrecognized one fails the record" was narrower than the failure surface — a bold label colliding with a *recognized* field also fails, as a duplicate-field error. Reworded the comment in both templates to "an unexpected one (unknown or duplicate) fails the record" and repackaged.

## Evidence

- Full suite green via `PATH=/tmp/pyshim:$PATH bash tests/run-all.sh` (exit 0): budget, schema, work-record, checker, redline, tuner, hooks, links, package.
- Diff is exactly the two template comments + regenerated `dist/`/`.claude/skills/` copies + manifest byte-count updates. Skill files reverted with no residual diff.
