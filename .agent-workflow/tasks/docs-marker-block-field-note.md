<!-- agent-workflow:start -->
**Outcome:**
Work Record authors are warned that `**Label:**` at the start of a line inside the marker block is reserved for fields — the parser treats any such line as a field and rejects unknown ones — so bold sub-headings inside a field's prose value (e.g. Plan) will not silently break the record. The guidance lands in both Work Record templates, `operating-mode.md`, and the SKILL principle; `dist/` regenerated.

**Target:**
This repository. Source: `core/templates/work-record-routine.md`, `core/templates/work-record-expanded.md`, `core/skill/operating-mode.md`, `core/skill/agent-workflow.md`. Generated `dist/agent-workflow/**` + `.claude/skills/**` regenerated by the packager.

**Scope:**
Documentation only — one HTML comment near the start marker of each template, one clause on the operating-mode "surrounding prose" line, and one clause on the SKILL "structured surface" principle. Then repackage. No parser/checker behavior change.

**Constraints:**
Fix source only; never hand-edit `dist/` — regenerate via `scripts/package-skill.sh` so `manifest.txt` stays honest. No change to `core/work_record/parser.py` or any predicate. Preserve the templates' existing field set and marker structure exactly.

**Completion criteria:**
Both templates carry the reserved-`**Label:**` note; `operating-mode.md` and the SKILL principle state the corollary; `dist/` + `.claude/skills/` copies regenerated; `bash tests/run-all.sh` green including the `package` drift check.

**Risk:** Routine
**Complexity:** Simple
**Reason:**
All touched paths are watch-zone in this repo's redline policy (`core/**`, `core/templates/**`, `dist/**`, `.claude/skills/**`); `docs/**` untouched. No red/gray/boundary/surface touch, no code or contract change — pure documentation. Watch-only pre-edit verdict → Routine; single mechanical change → Simple. `(Routine, Simple)` → compact shape.

**Approach:**
Add a concise HTML comment after the `<!-- agent-workflow:start -->` marker in both templates noting that a bold `**Label:**` at line-start is parsed as a field (unknown ones are rejected), so keep bold sub-headers out of a field's prose. Extend the operating-mode "surrounding prose is free-form" sentence and the SKILL "structured surface" principle with the same corollary. Regenerate `dist/` via the packager (python3 shim).

**Verification:**
`PATH=/tmp/pyshim:$PATH bash tests/run-all.sh` → all layers ok, especially `package` (dist matches source after regenerate) and `links`. `git diff` shows only the four source docs + their regenerated `dist/`/`.claude` copies.

**State:** Ready to implement
<!-- agent-workflow:end -->

## Implementation

- Discover/Assess: paths are watch-zone per `agent-redline-policy.yaml`; classified `(Routine, Simple)`, compact shape. Motivated by a real papercut — a consumer (Pallium) Work Record failed CI when a bold sub-heading inside the Plan prose was parsed as an unknown field (`core/work_record/parser.py` `_FIELD_HEADER_RE` matches any line-start `**X:**`). The parser behavior is correct-by-design; this task only documents the constraint so authors avoid the trap.
