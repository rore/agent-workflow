<!-- agent-workflow:start -->
**Outcome:**
Work Record authors are warned that `**Label:**` at the start of a line inside the marker block is reserved for fields — the parser treats any such line as a field and rejects unknown ones — so bold sub-headings inside a field's prose value (e.g. Plan) will not silently break the record. The guidance lands as an HTML comment in both Work Record templates; `dist/` regenerated.

**Target:**
This repository. Source: `core/templates/work-record-routine.md`, `core/templates/work-record-expanded.md`. Generated `dist/agent-workflow/**` + `.claude/skills/**` regenerated by the packager.

**Scope:**
Documentation only — one HTML comment near the start marker of each template. Then repackage. No parser/checker behavior change. (Originally also planned a clause in `operating-mode.md` and the SKILL principle; dropped — see Implementation — because the token-budget layer showed both agent-loaded files are at their ceilings and additions would need cutting existing content, which is out of scope.)

**Constraints:**
Fix source only; never hand-edit `dist/` — regenerate via `scripts/package-skill.sh` so `manifest.txt` stays honest. No change to `core/work_record/parser.py` or any predicate. Preserve the templates' existing field set and marker structure exactly. Do not push any agent-loaded file over its token budget.

**Completion criteria:**
Both templates carry the reserved-`**Label:**` note; `dist/` + `.claude/skills/` copies regenerated; `bash tests/run-all.sh` green including the `budget`, `package`, and `links` layers.

**Risk:** Routine
**Complexity:** Simple
**Reason:**
All touched paths are watch-zone in this repo's redline policy (`core/**`, `core/templates/**`, `dist/**`, `.claude/skills/**`); `docs/**` untouched. No red/gray/boundary/surface touch, no code or contract change — pure documentation. Watch-only pre-edit verdict → Routine; single mechanical change → Simple. `(Routine, Simple)` → compact shape.

**Approach:**
Add a concise HTML comment after the `<!-- agent-workflow:start -->` marker in both templates noting that a bold `**Label:**` at line-start is parsed as a field (unknown ones are rejected), so keep bold sub-headers out of a field's prose. Regenerate `dist/` via the packager (python3 shim).

**Verification:**
`PATH=/tmp/pyshim:$PATH bash tests/run-all.sh` → all layers ok, especially `budget` (no file over ceiling), `package` (dist matches source after regenerate), and `links`. `git diff` shows only the two templates + their regenerated `dist/` copies.

**State:** Ready for review
<!-- agent-workflow:end -->

## Implementation

- Discover/Assess: paths are watch-zone per `agent-redline-policy.yaml`; classified `(Routine, Simple)`, compact shape. Motivated by a real papercut — a consumer (Pallium) Work Record failed CI when a bold sub-heading inside the Plan prose was parsed as an unknown field (`core/work_record/parser.py` `_FIELD_HEADER_RE` matches any line-start `**X:**`). The parser behavior is correct-by-design; this task only documents the constraint so authors avoid the trap.
- Implement: initially added the note to four files (two templates + `operating-mode.md` + the SKILL principle in `agent-workflow.md`). The `budget` test layer then failed — `agent-workflow.md` 523/500 and `operating-mode.md` 1956/1900 tokens; both agent-loaded files were already at ceiling. Reverted the two skill-file additions (cutting existing content to make room was out of scope) and kept the templates-only note, which is the higher-value placement anyway (authors see it inline while filling in fields). Templates are not budget-constrained.
