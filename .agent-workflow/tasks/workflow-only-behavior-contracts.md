<!-- agent-workflow:start -->
**Outcome:** Repositories without branch protection or CODEOWNERS can opt into workflow-only behavior-contract protection that keeps red classification, semantic change records, explicit user approval, and PR CI verification without claiming merge enforcement.

**Target:** agent-workflow.

**Scope:** Normative behavior-contract model; Redline policy schema, reporter payload/reporting, Agent Workflow checker consumption, bootstrap choice and explanation, tests, public documentation, and generated/install artifacts.

**Constraints:** Existing behaviorContracts configurations remain repository-protected by default and unchanged. Workflow-only protection remains explicit, requires a PR CI job that runs the named verification, omits CODEOWNERS/required-status/branch-protection claims, and does not create a second path classifier, checkpoint system, or approval registry.

**Completion criteria:** A repository can explicitly choose workflow-only protection; protected paths remain red and require per-path classification, requirement changes require exact user approval, the named PR CI verification is linked and visibly fails when broken, reports disclose that GitHub does not block merge, strict existing configurations behave unchanged, and bootstrap explains and tests both choices.

**Requirement baseline:** {"source":"user-request:0365fa2e-e664-4a7c-89ed-d51839cce546","outcome":"Repositories without branch protection or CODEOWNERS can opt into workflow-only behavior-contract protection that keeps red classification, semantic change records, explicit user approval, and PR CI verification without claiming merge enforcement.","scope":"Normative behavior-contract model; Redline policy schema, reporter payload/reporting, Agent Workflow checker consumption, bootstrap choice and explanation, tests, public documentation, and generated/install artifacts.","constraints":"Existing behaviorContracts configurations remain repository-protected by default and unchanged. Workflow-only protection remains explicit, requires a PR CI job that runs the named verification, omits CODEOWNERS/required-status/branch-protection claims, and does not create a second path classifier, checkpoint system, or approval registry.","completion_criteria":"A repository can explicitly choose workflow-only protection; protected paths remain red and require per-path classification, requirement changes require exact user approval, the named PR CI verification is linked and visibly fails when broken, reports disclose that GitHub does not block merge, strict existing configurations behave unchanged, and bootstrap explains and tests both choices."}

**Risk:** High

**Complexity:** Moderate

**Reason:** Redline classifies the normative spec and policy schema as red architecture contracts. This opt-in relaxation changes protection and approval semantics across schema, reporter, checker, bootstrap, and packaged consumers; a wrong default could silently weaken existing repositories.

**Discovery:** Redline already owns behavior-contract path matching, red precedence, CODEOWNERS resolution, checkpoint routing, and versioned verdict evidence. Agent Workflow consumes that evidence to require one semantic classification per changed path, repository-owner approval for requirement changes, and a Work Record reference to the named verification. The strict policy schema currently requires `paths`, `verification`, and a dedicated CODEOWNER-only `checkpoint`; the reporter always emits version 1 with owners/checkpoint; the checker therefore cannot represent a contract without repository governance. Bootstrap currently omits all candidates unless required-status, CODEOWNERS, and required-review evidence are present. Existing Work Record parsing already supports the exact `task-owner` / `user` approval shape needed for workflow-only repository-contract changes, so no approval schema or registry is needed.

**Material assumptions:** 1. Assumption: omitted `protection` and explicit `repository` can preserve the exact version-1 payload and authorization behavior. Disproving evidence: any existing valid policy, verdict consumer, or test changes shape or semantics. Action: stop implementation and return to compatibility design. 2. Assumption: bootstrap can prove separately that the named behavior verification and the combined Redline/Agent Workflow harness both run on pull requests, without treating branch-required status as equivalent. Disproving evidence: either PR execution surface is missing or the evidence cannot distinguish running from branch-required. Action: omit/defer behaviorContracts, report it unresolved, and never silently downgrade. 3. Assumption: workflow protection can reuse the existing `authority.scope: task`, `authority.name: task-owner`, `approval.by: user` record as task-scoped approval evidence without changing Work Record syntax. Disproving evidence: parser/checker behavior requires a schema change or cannot bind approval to exact before/after values. Action: stop and return to planning; do not create a new authority registry implicitly. 4. Assumption: a strict version-2 evidence shape can omit repository-only owner/checkpoint data while retaining independently triggered Redline checkpoints. Disproving evidence: downstream consumers require those fields or unrelated checkpoint routing is lost. Action: stop and redesign the evidence boundary. `protection: workflow` remains distinct from Redline shadow/binding: the workflow blocks readiness, but neither the checker nor an unrequired CI job authenticates the user or prevents a manual merge.

**Plan:** 1. Amend SPEC first and add a superseding decision: `behaviorContracts.protection` accepts `repository` or `workflow`, defaults to `repository`, and changes governance only—not Redline path ownership, red precedence, semantic classification, exact approval binding, or verification linkage. State normatively that workflow-mode task-owner/user evidence is task-scoped and unauthenticated, not repository authority, and that GitHub may allow manual merge. 2. Extend the schema and reporter conditionally: omitted/explicit repository protection requires its dedicated CODEOWNER-only behavior checkpoint and emits the exact existing version-1 object; workflow protection forbids only that behavior checkpoint, skips behavior-contract owner lookup/routing, retains unrelated Redline checkpoints, stays red, and emits exactly version 2 as `{version, protection: workflow, detected, paths: [string], verification}`. 3. Extend the verdict parser/checker to accept only v1/repository and v2/workflow pairings, reject forged cross-mode/stale/malformed detail, preserve strict authorization unchanged, and require the existing exact `task-owner` / `user` evidence for workflow requirement changes. Keep trusted changed-path agreement, red classification, one semantic classification per path, and verification linkage blocking and non-waivable. 4. Update bootstrap discovery and sequencing: explain both choices and their enforcement boundary; require explicit selection; prove separately that the named verification and combined harness run on PRs; persist workflow protection only after both are present or confirmed installed. If either is absent or Phase 5 remains proposal-only, omit/defer the block and report it unresolved. Never downgrade repository protection automatically. 5. Update both reporter rendering and the shipped workflow template's actual Redline sticky-comment path so every workflow-mode contract change says it runs in PR CI but is not merge-enforced. 6. Add focused schema/reporter/verdict/checker tests and packaged bootstrap E2E coverage: omitted and explicit repository modes produce exact v1; strict layout remains governed; workflow mode works without CODEOWNERS in the second noncanonical layout; unrelated checkpoints survive; approval, warning, detected-false, duplicate/extra/unsafe paths, forbidden checkpoint, unknown protection, stale verification, and version-mode mismatch cases fail correctly. 7. Update public/reference docs, regenerate and locally install packaged artifacts, run focused tests and `bash tests/run-all.sh`, then perform clean-context result review before delivery. Unresolved decisions: none. Architecture deviation: workflow protection is an explicit weaker hosting-governance option; repository protection remains the default.

**Verification plan:** Existing repository protection shall remain schema-valid, emit byte-for-shape version-1 behavior detail, and enforce CODEOWNERS/checkpoint authorization → schema, reporter, verdict-parser, checker, and packaged strict E2E assertions. Explicit workflow protection shall require paths plus a nonempty PR-CI verification identifier, reject a behavior checkpoint, classify matching excluded/blue paths red, emit version-2 evidence without owner/checkpoint ceremony, require exactly one semantic entry per path, require exact task-owner/user approval only for `requirement-change`, require the verification identifier in an affected Work Record, and render the no-merge-enforcement warning → focused unit/integration tests. Bootstrap shall show both choices, distinguish a running PR check from a branch-required check, require explicit selection, and generate a working policy in a second repository layout → packaged bootstrap E2E. Generated sources shall match dist/install outputs → package checks. Entire repository remains green → `bash tests/run-all.sh`.

**Plan review:** Approved on re-review by clean-context High-risk reviewer `/root/workflow_mode_plan_review`. The revision closed bootstrap sequencing, unauthenticated task-scoped approval semantics, the actual shipped sticky-renderer surface, and assumption-specific stop conditions. The reviewer found no remaining plan blockers; the user approval is recorded below.

**Approvals:** Approved by user 2026-09-22: "Yes"

**Exceptions:** —

**State:** Ready to implement
<!-- agent-workflow:end -->

## Implementation

- Established Task Context and pre-edit classification before implementation. The change is not documentation-only because schema, reporter, checker, bootstrap, and normative specification are in scope.
- Plan approved; implementation targets are `docs/SPEC.md`, `docs/DECISIONS.md`, behavior-contract public/reference docs, Redline policy schema and reporter, checker/verdict parsing, bootstrap instructions, the shipped PR workflow template, focused tests, and generated install artifacts. No unrelated policy or workflow behavior is in scope.
- Architecture checkpoint: add explicit workflow-only behavior-contract protection while preserving repository protection as the compatible default; migration risk is high because incorrect defaults could silently weaken governance. Verification is the compatibility/malformed-evidence matrix, packaged two-layout E2E, generated-package parity, and full suite.

## Evidence

- Clean checkout on `feat/workflow-only-behavior-contracts` from `ff081714806504284c61051594103145ca7bfd88`.
- Clean-context pre-edit Redline review: RED/SCHEMA_CHANGE, architecture-review, no boundary violation, High risk floor.

## Plan review

Pending.

## Result review

Pending.
